=========
Reference
=========

Lookup pages for the JSON output contract and the processors' options.

*   `JSON contract <JsonContract.rst>`__ — the exact output shape per field type, frozen by characterization tests
*   `Normalizers and transformers <Normalizers.rst>`__ — built-in services, interfaces and DI tags
*   `Processor options <ProcessorOptions.rst>`__ — TypoScript options of `nb-content-blocks-json` and `nb-container-json`

..  toctree::
    :hidden:
    :titlesonly:

    JsonContract
    Normalizers
    ProcessorOptions
